Authorize your Stellar Transaction

Go to: <%- locals.authorizeUrl %> to view, authorize, and submit this transaction.

<% if(locals.code) { %>
You authorization code is: <%= locals.code %>

This code is valid for 24 hours
<% } %>